---
"@nx.js/runtime": patch
---

Use native applet resume events for socket wake recovery, honor explicit applet exit requests on GPU rendering paths, and guard controller polling when HID loses its shared-memory mapping.
