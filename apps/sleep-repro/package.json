{
  "name": "sleep-repro",
  "version": "0.0.0",
  "private": true,
  "description": "Minimal sleep/wake console-crash reproduction (see README.md)",
  "scripts": {
    "build": "esbuild --bundle --sourcemap --sources-content=false --target=es2022 --format=esm --outdir=romfs src/main.ts",
    "nro": "nxjs-nro"
  },
  "license": "MIT",
  "devDependencies": {
    "@nx.js/nro": "workspace:^",
    "@nx.js/runtime": "workspace:^",
    "esbuild": "catalog:"
  }
}
